import { ControladordeimgPipe } from './controladordeimg.pipe';

describe('ControladordeimgPipe', () => {
  it('create an instance', () => {
    const pipe = new ControladordeimgPipe();
    expect(pipe).toBeTruthy();
  });
});
